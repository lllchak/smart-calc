#ifndef SRC_STACK_UTILS_H_
#define SRC_STACK_UTILS_H_

#include "../types.h"

/**
 * @brief      Prints out stack values
 * 
 * @param stk  Pointer to stack object to print out
 * @return     None (only prints stack values)
 */
void print_stack(stack* stk);

#endif  // SRC_STACK_UTILS_H_